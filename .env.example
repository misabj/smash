# Server port
PORT=3001

# JWT secret - CHANGE THIS to a strong random string in production!
JWT_SECRET=promenime_jako_dugacak_random_string_1234567890

# CORS origins (comma-separated, leave empty in production if frontend is served from same origin)
# CORS_ORIGIN=https://tvojdomen.rs

# Node environment
NODE_ENV=production
